#ifndef WINDOW_REWARDS_H
#define WINDOW_REWARDS_H
#include "../common.h"

Container rewardsContainer;
Container rewardBoard;
Container rewardLogo;
Container rewardBox;
int currReward;
void setRewardContainer(int level);
void drawRewardContainer();
#endif // WINDOW_REWARDS_H